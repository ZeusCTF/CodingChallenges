# LeetCode Solutions

Solutions to LeetCode problems in Python and C.

- Python solutions are in `python/` and named `<number>_<slug>.py`
- C solutions are in `c/` and named `<number>_<slug>.c`

Each file contains the problem description, a brief explanation of the approach, and the solution.
